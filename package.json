{
  "name": "quorumq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix src/api\" \"npm run dev:web\"",
    "dev:web": "npm run dev --prefix src/web",
    "build": "npm run build --prefix src/api && npm run build --prefix src/web",
    "test:e2e": "npm run test --prefix tests/e2e",
    "lint:design": "npm run test --prefix tests/e2e -- --grep \"L2-24\""
  }
}
